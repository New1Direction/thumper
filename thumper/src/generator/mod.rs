//! Generator engine + python bridge (real RedMicro integration) + native emitters for Rust/Go.

pub mod native;
pub mod python_bridge;

// Common types shared across backends (python bridge + native).
